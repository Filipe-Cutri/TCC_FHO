# Spring Boot Application Configuration (YAML format)
# This file demonstrates YAML configuration alongside the existing .properties files

spring:
  application:
    name: slotify-backend
  
  profiles:
    active: dev
  
  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/slotify
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  # JPA/Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

# Server Configuration - HTTPS/SSL
server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:slotfy.p12
    key-store-password: slotfypass
    key-store-type: PKCS12
    key-alias: slotfy

# Logging Configuration
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE