[phases.setup]
nixPkgs = ["...", "jdk17"]

[phases.build]
cmds = [
    "cd back-end",
    "./gradlew clean build -x test"
]

[phases.install]
dependsOn = ["setup"]

[start]
cmd = "cd back-end && java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar build/libs/*.jar"
