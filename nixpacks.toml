[phases.setup]
nixPkgs = ['openjdk17']

[phases.build]
cmds = ['cd back-end && ./gradlew clean build -x test']

[start]
cmd = 'java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar back-end/build/libs/slotify-backend-*.jar'
